[project]
name = "pyforge"
version = "0.1.0"
description = "A Python developer productivity CLI"
requires-python = ">3.10"
[tool.pytest.ini_options]
testpaths = ["tests"]
[tool.coverage.run]
source = ["src/pyforge.py"]